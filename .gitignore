# Binaries
bin/
# A stray top-level manager binary (e.g. from `go build -o manager`).
# Root-anchored so it does NOT match the cmd/manager/ source directory.
/manager

# Test artifacts
*.test
cover.out
coverage.out

# Generated tooling installed by the Makefile
/bin

# Editor / OS
.DS_Store
*.swp
.idea/
.vscode/
